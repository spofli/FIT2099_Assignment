Winning and losing was implemented by adding attributes and methods to the scheduler. This allows for the reuse of the engine with the win lose implemented. When a game ending scenario is triggered, a method will change the status of an attribute which affects tick(). This will break the running of scheduled actions and jump to back the application where tick is called and print the desired endgame text,